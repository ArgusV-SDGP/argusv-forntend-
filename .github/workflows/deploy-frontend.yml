name: Deploy Frontend

on:
  push:
    branches: [main]
    paths:
      - "argusv-forntend-/**"
      - ".github/workflows/deploy-frontend.yml"

env:
  REGISTRY: registry.digitalocean.com/${{ secrets.DOCR_NAME }}
  IMAGE_NAME: argusv-frontend

jobs:
  build-and-push:
    name: Build & Push Image
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.tag }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Log in to DOCR
        run: doctl registry login --expiry-seconds 1200

      - name: Set image tag
        id: meta
        run: echo "tag=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}" >> $GITHUB_OUTPUT

      - name: Build image
        # NEXT_PUBLIC_* vars are baked into the JS bundle at build time
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_BASE_URL=${{ secrets.NEXT_PUBLIC_BASE_URL }} \
            --build-arg NEXT_PUBLIC_STREAM_BASE_URL=${{ secrets.NEXT_PUBLIC_STREAM_BASE_URL }} \
            -t ${{ steps.meta.outputs.tag }} \
            argusv-forntend-/

      - name: Push image
        run: |
          docker push ${{ steps.meta.outputs.tag }}
          docker tag ${{ steps.meta.outputs.tag }} ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

  deploy:
    name: Deploy to Droplet
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Ensure server directories exist
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          script: mkdir -p /opt/argusv/nginx

      - name: Sync compose file to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          source: "argusv/docker-compose.prod.yml"
          target: "/opt/argusv/"
          strip_components: 1

      - name: Sync nginx config to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          source: "argusv/nginx/default.conf"
          target: "/opt/argusv/nginx/"
          strip_components: 2

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          script: |
            set -e
            cd /opt/argusv

            # Authenticate to DOCR on the server
            doctl registry login --expiry-seconds 600

            # Pull only the frontend image and restart it
            docker compose -f docker-compose.prod.yml --env-file .env.prod pull frontend
            docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --no-deps frontend

            # Reload nginx to pick up any config changes
            docker compose -f docker-compose.prod.yml --env-file .env.prod exec -T nginx nginx -s reload || true

            # Remove dangling images to free disk
            docker image prune -f

            echo "✅ Frontend deployed: ${{ needs.build-and-push.outputs.image_tag }}"
